import MenuRouteWrapper from '@/lib/components/shared/Layout/MenuRouteWrapper'

import { deliveryTransactionRoutes } from './routes'

const DeliveryTransactionPages = () => {
  return <MenuRouteWrapper routes={deliveryTransactionRoutes} />
}

export default DeliveryTransactionPages
